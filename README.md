# Hello World

A simple Node.js Express application designed for EKS deployment with health checks and graceful shutdown.

Run locally: `npm install && npm start` | Build container: `docker build -t hello-world .`

